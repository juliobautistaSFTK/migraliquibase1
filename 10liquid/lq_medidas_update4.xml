<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
                   xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                   xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.0.xsd">

    <!-- ChangeSet para la carga de datos -->
    <changeSet id="agrego_registro_4" author="usuario1">

        <!-- Inserta datos en la tabla -->
        <sql>
            <!-- Cambia 'nombre_de_tabla' por el nombre real de tu tabla -->
            INSERT INTO medidas2 (idd, tipo, cantidad) VALUES
            (4, 4, 40);
        </sql>
	<rollback>
            DELETE FROM medidas2 WHERE idd = 4;
        </rollback>

    </changeSet>
</databaseChangeLog>
